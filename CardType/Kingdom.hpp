#ifndef __Kingdom_hpp
#define __Kingdom_hpp
#include<string>
#include<iostream>
#include "Model.hpp"
using namespace std;

class Kingdom {
  private:
  public:
    Kingdom(string nom, int valeur, int cout, string description);
    ~Kingdom();
    // void affiche();
    // void action();
    // void onDraw(Joueur* JoueurActuel) override;
    // void onPlay(Joueur* JoueurActuel)const override;
    // void onDiscard(Joueur* JoueurActuel) override;
    // void onTrash(Joueur* JoueurActuel) override;
    // void onAddToDeck(Joueur* JoueurActuel) override;
    
};

#endif